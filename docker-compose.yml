services:
  app:
    #build:
    #  context: ./app
    #  dockerfile: Dockerfile
    #mem_limit: 200m
    image: project-rag-app:latest
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app
    environment:
      - PYTHONUNBUFFERED=1
      - TZ=Asia/Shanghai
    #restart: unless-stopped
